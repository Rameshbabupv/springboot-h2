package com.hrms.enums;

/**
 * Defines whether leave is for full day or half day.
 */
public enum LeaveDayType {
    FULL,        // Full day leave
    FIRST_HALF,  // Morning half (typically before lunch)
    SECOND_HALF  // Afternoon half (typically after lunch)
}
